{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open HTML file in Chrome",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}